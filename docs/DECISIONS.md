# 业务与架构决策

> 根目录保护文档：永久保留、按需更新；未经用户明确点名要求删除，不得删除、改名、移动、清空或用空模板覆盖。新会话不必通读本文件，按任务读取。
> 本文依据 0921 导出包；其 2,300 个普通文件与 0920 包逐文件字节一致。历史修复报告不等于本包已修复，状态以 PROJECT_STATUS.md 为准。


本文件防止 AI 把猜测写成规则。下列建议均未获得本次用户逐项确认；任务触及相应决策时才询问，不一次性把所有问题抛给用户。

## 已核实的技术事实

- 项目采用现有 Vben + Nitro 结构；数据库运行入口为 utils/db.ts。
- 当前有两种数据库分支、后端菜单模式、业务数据与磁盘附件。
- 上述是源码事实，不是对其正确性或未来不可改变的承诺。

## 待定事项

| 编号 | 要决定的问题 | 当前代码 / 冲突 | 建议与影响 | 状态 |
|---|---|---|---|---|
| D01 | admin 与 super 是否拥有相同特权？ | admin 默认 allReadOwnWrite，但 isSuper 将其视为全权 | 建议只有 super 全权；admin 按明确权限与读写范围。会改变既有操作范围，需业务确认 | 待确认 |
| D02 | 多角色如何合并？只读能否被另一角色的写权限扩展？ | dataScopes 按数组判断，缺独立写范围；旧文档只读表述不完整 | 明确角色级授权与数据范围组合；不要让只读角色的全量读取被另一角色写权限扩成全量写入 | 待确认 |
| D03 | 普通销售可看全公司销售分析吗？ | analytics/sales.get.ts 当前返回全公司商机汇总 | 默认建议遵循业务数据范围；若全员共享，应明确哪些汇总可共享，避免明细泄露 | 待确认 |
| D04 | 合同金额与明细/计划是否必须一致？哪些财务状态计入统计？ | 比例有校验，金额和回款状态存在多种输入 | 确认舍入、超额、预付款、质保金、作废/退款规则；开票和回款分别统计 | 待确认 |
| D05 | 客户移交权限是否允许移交所有客户，合同是否一起转移？ | 当前选取客户不按范围过滤，只同步客户和进行中商机 | 明确可移交范围、接收人资格、撤回条件、合同负责人是否保持；不要自动推断 | 待确认 |
| D06 | 普通销售能否新建客户？ | 默认动作有 customer:create，但新增接口只允许 isSuper；导入更宽松 | 统一新建、导入与负责人分配策略，避免按钮可见但永远失败 | 待确认 |
| D07 | 员工档案与系统账号是否联动？ | hr_employee 与 sys_user 分开；档案部门岗位存名称 | 默认保持独立；如需联动，明确建立账号、停用、离职和历史数据规则 | 待确认 |
| D08 | 框架恢复备份如何维护？ | packages 和 vendor-vben 有两处差异，marker 缺失即全量恢复 | 建议保留现有平台必要恢复能力，加入审阅后的同步及内容校验；不要先删备份 | 技术建议待实施验证 |
| D09 | 正式环境运维责任与恢复目标是什么？ | 源码有卷与部署配置，无备份运行证据 | 明确谁负责备份、允许丢失多久数据、多久恢复，安排独立演练 | 待确认 |

## 决策记录格式

每项结论记录：日期、用户确认内容/依据、最终规则、影响模块、代码/迁移变更、验证证据、替代了哪条旧规则。未确认建议不得标为已决定。

产品规则发生变化时更新 CORE_RULES.md，执行任务进度更新 PROJECT_STATUS.md，不在此复制任务列表。
