# 当前状态与任务（新会话必读）

更新：2026-09-21。权威范围：0921-92e9f48e.tar.gz 导出内容；不等于平台此刻的运行状态。本文件永久保留在根目录。

## 重要：导出包未包含历史修复

本次逐文件比较：0921 与 0920 两包均有 2,300 个普通文件，文件路径和内容完全一致；压缩包 SHA-256 不同不代表源码有变化。

本包确认仍存在：
- 合同更新/删除使用 verifyAccessToken，再把结果传给 dataScopeCondition（R01）。
- 员工和回款以 create 权限进入 UPSERT（R03）。
- 启动覆盖角色/组织、补回授权（R04）；旧产物和迁移标记问题（R07/R08）。
- 没有新 AGENTS、八份核心文档、上一轮新增测试；没有 data/ 与 node_modules。

历史对话中的路小飞报告称：R01/R03 已修复，隔离路由 24 项、源码约束 5 项、真实 Nitro HTTP + PGlite 54 场景通过；旧预览未更新。用户随后授权执行 R04，但本次没有提供其完成结果。

上述历史报告与本包存在版本差异。不能把历史通过状态套用到本包，也不能断言平台已经丢失全部修改。可能是导出版本、会话、快照或持久化问题；原因待核实。

## 下一步优先事项

**P0：先核对平台当前工作区与导出来源，保存文档和实际修复成果。**只读核对关键文件、测试与 Git 状态；若平台仍有修复则保留并导出正确版本，不重复修复；若确实退回旧版，查找可验证的修改副本，之后才安排恢复/重新修复。禁止用本包覆盖平台较新的源码。

## 核心修复账本

| 编号 | 范围/证据入口 | 本包状态 | 优先级与验收 |
|---|---|---|---|
| R01 | contract/[id].put.ts、[id].delete.ts；rbac.ts | 未包含历史修复 | 必修；完整上下文、update/delete 权限、越界拒绝、普通用户无 500 |
| R03 | hr/employee/index.post.ts、contract/payment/index.post.ts | 仍是 create + UPSERT | 必修；新增编辑分离、旧记录与新引用校验、创建人保留 |
| R04 | system-persistence.ts、base-seed.ts | 风险仍在；平台执行结果未知 | 必修；重启后撤权、停用、角色范围、组织修改不被覆盖 |
| R05 | rbac.ts、permission-policy.ts、middleware/2.rbac.ts | admin 等同 super，读写语义待定 | 必修；D01/D02 确认后落实，不能猜业务政策 |
| R02 | customer/import.post.ts、contract/file、analytics/sales.get.ts | 登录/资源授权或数据范围不足 | 必修；附件、导入、统计权限统一；统计口径见 D03 |
| R06 | customer-transfer/index.post.ts、[id]/reverse.put.ts | pg.Pool 分支无完整事务 | 必修；同 client 事务、失败回滚、移交范围见 D05 |
| R07/R08 | dev-start.sh、prepare-runtime.sh、migrate.mjs | 锁等待、旧产物、全局迁移标记风险 | 必修；安全重启加载新构建，新增迁移不跳过 |
| R09 | packages/effects/access/src 与 vendor-vben 对应路径 | directive.ts、use-access.ts 两处不同 | 恢复前必修；恢复不得回退权限修复。自动同步工具可延后 |
| R10 | web-antd/src/router/access.ts | 前端仍补入合同/发票菜单 | 与权限任务合并，菜单不可因补丁绕过撤权 |
| R11 | core-flow.test.ts、vitest.config.ts | 兜底金额断言、错误清理、未隔离 | 验收前修必要部分；无需全面重写框架测试 |
| R12 | 校验、导入、金额、审计、示例 | 尚未统一 | 导入原子性/核心金额规则上线前处理；无关整理延后 |

上表后端路径默认相对 apps/server/api 或 utils，详见 PROJECT.md/SOURCE_INVENTORY.md。

## 节省 Token 的推进方式

按依赖合并为：版本与保留核对 → 初始化/运行迁移 → 权限与数据范围 → 事务及核心业务 → 集中回归/备份恢复/受控应用。每批连续完成修改、测试、记录，不重复读完整清单或复制旧报告。UI、美化、框架升级、非阻塞历史类型错误延后。

不得为减少轮次省略关键测试。R09 的恢复回退风险不属于纯美化，应在启用恢复机制前解决。

## 待确认业务问题

D01 admin/super；D02 多角色读写范围；D03 统计可见范围；D04 金额与状态；D05 移交范围；D06 销售新建客户。任务触及时只读取 DECISIONS.md 对应行。

## 记录要求

每轮仅记日期、任务、源码/构建版本证据、验证环境与结果、未验证项、是否应用原预览。没有运行证据不得写“已验证”；无需再次堆积长报告。

2026-09-21：完成导出包路径/内容比较和关键实现复核；重建九份根目录文档。未修改业务源码、未安装依赖、未运行平台服务、未写业务数据库。文件保留配置尚待平台执行和快照验证。
